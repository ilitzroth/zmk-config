build:
  # Vendored behavior: zmk-behavior-battery-percentage-printer (&bapp).
  # Source pinned from badjeff/zmk-behavior-battery-percentage-printer @ e111cbc.
  cmake: .
  kconfig: Kconfig
  settings:
    board_root: .
    dts_root: .
